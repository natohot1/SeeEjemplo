<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.seeejemplo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IndiceAsma4"></activity>
        <activity
            android:name=".Main4Activity"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".Asma2"
            android:screenOrientation="fullSensor" />
        <activity android:name=".Inhalador" />
        <activity android:name=".BanderaActivity" />
        <activity
            android:name=".Asma"
            android:screenOrientation="fullSensor" />
        <activity android:name=".IndiceGravedadAsma" />
        <activity
            android:name=".FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_fullscreen"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".Segundo" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>